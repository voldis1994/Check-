# Check

Production trading system lives in [`Check/System`](Check/System).

**SYSTEM v2.0.0** — Python + MetaTrader 4 file-bridge (`python -m checktrader`).

See `Check/System/README.md` for install, live start, and protocol docs.

Technical operability is not a profit guarantee.
