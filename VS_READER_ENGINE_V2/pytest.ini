[pytest]
addopts = --import-mode=importlib
pythonpath = .
markers =
    no_ai_mock: skip autouse OpenAI mock to test advisory/required AI behavior
    deployment: path contract tests against the real deployment layout (run after sync_paths)
